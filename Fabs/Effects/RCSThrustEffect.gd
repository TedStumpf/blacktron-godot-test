extends Particles2D

export (bool) var isForward = false;
export (bool) var isBackward = false;
export (bool) var isLeft = false;
export (bool) var isRight = false;
export (bool) var isCW = false;
export (bool) var isCCW = false;
